//code for session indicator project

//stepper motor library

//rtc library

int current_position;
int next_position;
int current_day;



void setup() {
  // put your setup code here, to run once: 

  // set up stepper motor
  
  // set up rtc
 
}

void loop() {
  // put your main code here, to run repeatedly:
  // check time
  // if statement, if current time is equal to target time, call rotate function
  if (current_time == ){
    // declare current_time, current_day
    // pass values to rotate()
  }
  else{
    // do nothing
  }
}

void declare_position(current_time, current_day){
  // set up multidimensional array
  // switch function, use passed variables to determine case
  // each case contains a position in an array to get
}

void rotate(new_position){
  //rotate to new position
}
